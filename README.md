# Ally's Brain Index

A personal encyclopedia that syncs across all your devices.

## How It Works

- Topics sync across all devices via Firebase Firestore (real-time)
- The + button calls Claude via a Netlify serverless function to auto-generate entries
- Your Anthropic API key stays safe on the server
